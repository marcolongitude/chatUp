[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra -vv --tb=short
markers =
    pair: dual-device USB + emulator messaging
    smoke: fast sanity checks
    family: family-mode consent / list-control flows
    security: privacy anti-leak assertions (fail hard on destination exposure)
