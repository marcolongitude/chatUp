# Railway configuration for NestJS Backend
# Este arquivo configura o build e deploy do backend na Railway

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start:prod"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

