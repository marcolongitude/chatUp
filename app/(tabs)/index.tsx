import React from "react";
import { ContactList } from "@/widgets/contact-list";

/**
 * Página de Conversas (Contatos Próximos)
 * Camada: Pages (FSD)
 */
export default function ConversationsPage() {
	return <ContactList />;
}
